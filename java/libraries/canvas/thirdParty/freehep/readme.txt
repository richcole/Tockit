These libraries come from the FreeHEP project (http://java.freehep.org/).

Version is 1.2.1. Note that we need a little patch, which is given as a 
ZIP file containing both source and classfile. To use it put it into the
classpath in front of the other libraries from this directory.
